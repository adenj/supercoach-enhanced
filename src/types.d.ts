// Type declarations for browser WebExtension APIs
// This is a simplified version to allow the extension to compile correctly

declare namespace browser {
  // Add any browser APIs you need to use here
  // For now we're keeping it minimal since we're only logging to console
}